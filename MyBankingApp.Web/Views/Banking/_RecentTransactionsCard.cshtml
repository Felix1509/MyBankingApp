<div class="content-card">
    <div class="card-header">
        <div class="d-flex justify-content-between align-items-center">
            <h3>
                <i class="fas fa-clock"></i>
                Letzte Aktivitäten
            </h3>
            <a href="@Url.Action("Transactions", "Banking")" class="btn btn-light btn-sm">
                Alle
            </a>
        </div>
    </div>
    <div class="card-body transactions-grid">
        @Html.Action("RecentTransactionsGridPartial", "Banking")
    </div>
</div>